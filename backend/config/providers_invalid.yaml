# Invalid provider configuration for testing validation
# This file contains intentional errors to test validation logic

providers:
  - name: gemini
    adapter_type: app.providers.gemini.GeminiProvider
    enabled: true
    supported_models:
      - gemini-1.5-flash
    priority: 100
    timeout_seconds: 30.0
    retry_policy:
      max_attempts: 15  # Invalid: exceeds max of 10
      backoff_factor: 2.0
      initial_delay: 1.0
    health_check_settings:
      enabled: true
      interval_seconds: 60
      timeout_seconds: 5.0
      unhealthy_threshold: 3
      healthy_threshold: 1
    routing_weight: 1.0
    capabilities:
      streaming: true
      function_calling: true
      vision: true
      parallel_requests: true
    env_var_prefix: GEMINI

  - name: "invalid-name!!"  # Invalid: contains special characters
    adapter_type: invalid_module_path  # Invalid: not a valid module path
    enabled: true
    supported_models: []  # Invalid: empty list
    priority: 100
    timeout_seconds: 30.0
    routing_weight: 1.0
